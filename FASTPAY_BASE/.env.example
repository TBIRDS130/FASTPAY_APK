# Optional build-time config. Copy to .env in this folder and set values if needed.
# If .env is missing, BuildConfig uses the defaults below.

# Django API base URL (no trailing slash in value)
DJANGO_API_BASE_URL=https://api-staging.fastpaygaming.com/api

# Firebase Storage bucket
FIREBASE_STORAGE_BUCKET=fastpay-9d825.appspot.com
