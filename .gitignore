# Build outputs (repo-level)
releases/
APKFILE/
build/
node_modules/

# Docker cache directories
docker-cache/

# OS
.DS_Store
Thumbs.db

# Pre-commit / Python (when using pre-commit)
.venv/
__pycache__/
*.pyc

# Editor / IDE (root-level)
*.swp
*.swo
*~

# Firebase configuration (root-level - keep app-level version)
google-services.json
